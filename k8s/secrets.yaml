apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  namespace: interview-agent
type: Opaque
stringData:
  jwt-secret: "interview-agent-super-secret-jwt-key-2024-hackathon"
  # Replace with your MongoDB Atlas URI or keep as-is for in-cluster Mongo
  mongodb-uri: "mongodb://mongodb-service:27017/interview-agent"
